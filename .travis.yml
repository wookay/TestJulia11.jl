# Documentation: http://docs.travis-ci.com/user/languages/julia/
notifications:
  email: false

language: julia

os:
  - linux

julia:
  - 1.1

before_script:
  - julia --color=yes -e 'using Pkg; pkg"dev ASTInterpreter2 DebuggerFramework"'

script:
  - JIVE_PROCS=auto julia --color=yes -e 'using Pkg; Pkg.test()'
